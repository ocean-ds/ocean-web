import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import Loading from '../Loading';

<Meta
  title="Components/Loading"
  component={Loading}
  decorators={[(storyFn) => <div className="show-grid">{storyFn()}</div>]}
  parameters={{ layout: 'padded' }}
/>

# Loading API

The API documentation of the Loading React component. Learn more about the props and the CSS customization points.

## Import

```javascript
import { Loading } from '@useblu/ocean-react';
```

## Usage

<Canvas withSource="open" withToolbar>
  <Story name="usage">
    <div class="flex-story" style={{ backgroundColor: '#333' }}>
      <Loading />
    </div>
  </Story>
</Canvas>

## CSS

| Global class   | Description                                                                               |
| -------------- | ----------------------------------------------------------------------------------------- |
| .ods-list      | Styles applied to the root element.                                                       |
| .ods-list-item | Adds a bottom line to the item inside list, also user in TransactionLoadingItem Component |

If that's not sufficient, you can check the [implementation of the component](https://github.com/ocean-ds/ocean-web/blob/master/packages/ocean-react/src/Tag/Tag.tsx) for more detail.
