import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { useArgs } from '@storybook/client-api';
import DatePicker from '../DatePicker';

<Meta
  title="Components/DatePicker"
  component={DatePicker}
  decorators={[
    (storyFn) => <div style={{ maxWidth: '500px' }}>{storyFn()}</div>,
  ]}
  parameters={{ layout: 'centered' }}
/>

# DatePicker APIs

The API documentation of the DatePicker React component. Learn more about the props and the CSS customization points.

## Import

```javascript
import { DatePicker } from '@useblu/ocean-react';
```

```jsx
<DatePicker
  labels={{
    from: 'Start date',
    to: 'End date',
  }}
  values={{
    from: '10/08/2022',
    to: '15/08/2022',
  }}
  onSelect={(days) => ({ from: days.from, to: days.to })}
/>
```

## Usage

<Canvas withSource="open" withToolbar>
  <Story
    name="usage"
    args={{
      labels: { from: 'Start date', to: 'End date' },
      values: { from: '', to: '' },
      disabled: false,
      editable: false,
      error: false,
    }}
    argTypes={{
      labels: {
        name: 'labels',
        type: { name: 'text', required: false },
        defaultValue: { from: 'Start date', to: 'End date' },
        description: 'Dates from/to',
        control: { type: 'object' },
      },
      values: {
        name: 'values',
        type: { name: 'text', required: true },
        description: 'Values of from/to fields',
        control: { type: 'object' },
      },
      onSelect: {
        control: 'values',
      },
      disabled: {
        description: 'turn on component disabled',
        control: 'boolean',
      },
      editable: {
        control: 'boolean',
      },
      error: {
        control: 'boolean',
      },
    }}
  >
    {(props) => <DatePicker {...props} />}
  </Story>
</Canvas>

## props

<ArgsTable story="Usage" of="." />

<!-- ## CSS

| Global class            | Description                                                   |
| ----------------------- | ------------------------------------------------------------- |
| .ods-tag                | Styles applied to the root element.                           |
| .ods-tag--default       | Styles applied to the root element with a default type.       |
| .ods-tag--primary       | Styles applied to the root element with a primary type.       |
| .ods-tag--complementary | Styles applied to the root element with a complementary type. |
| .ods-tag--negative      | Styles applied to the root element with a negative type.      |
| .ods-tag--warning       | Styles applied to the root element with a warning type.       |
| .ods-tag--positive      | Styles applied to the root element with a positive type.      |
| .ods-tag\_\_icon        | Styles applied to the icon element.                           |
| .ods-tag\_\_content     | Styles applied to the content element.                        |

If that's not sufficient, you can check the [implementation of the component](https://github.com/ocean-ds/ocean-web/blob/master/packages/ocean-react/src/Badge/Badge.tsx) for more detail. -->

<!-- ## Playground

<Canvas>
  <Story
    name="playground"
    args={{
      children: 'Lorem ipsum',
    }}
  >
    {(props) => <DatePicker {...props} />}
  </Story>
</Canvas> -->
