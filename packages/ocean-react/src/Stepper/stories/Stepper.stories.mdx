import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import Stepper from '../Stepper';

<Meta title="Components/Stepper" component={Stepper} />

# Stepper API

The API documentation of the Stepper React component. Learn more about the props.

## Import

```javascript
import { Stepper } from '@useblu/ocean-react';
```

## Default Usage

<Canvas withSource="open" withToolbar>
  <Story name="usage">
    <Stepper step={1} off={4} />
  </Story>
</Canvas>

## Props

<ArgsTable of="." />

The ref is forwarded to the root element.

Any other props supplied will be provided to the root element (native element).

If that's not sufficient, you can check the [implementation of the component](https://github.com/ocean-ds/ocean-web/blob/master/packages/ocean-react/src/Stepper/Stepper.tsx) for more detail.

## Playground

<Canvas>
  <Story
    name="playground"
    args={{
      step: 1,
      off: 4,
    }}
    argTypes={{
      step: {
        options: [1, 2, 3, 4],
      },
      off: {
        options: [1, 2, 3, 4],
      },
    }}
  >
    {(props) => <Stepper id="stepper-playground" {...props} />}
  </Story>
</Canvas>
