---
title: InternalContextualHero
sidebar_label: InternalContextualHero
---

import { InternalContextualHero } from '@useblu/ocean-react';
import StorybookEmbed from '@site/src/components/StorybookEmbed';

# InternalContextualHero

Componente de destaque utilizado para exibir mensagens personalizadas baseadas na jornada do usuário. Permite combinar título, descrição, lista de itens, botões de ação e uma imagem opcional.

## Importação

```tsx
import { InternalContextualHero } from '@useblu/ocean-react';
```

### Importação de tipos TypeScript

```tsx
import type {
  InternalContextualHeroProps,
  InternalContextualHeroListItemString,
  InternalContextualHeroAction,
} from '@useblu/ocean-react';
```

## Playground Interativo

Explore o componente InternalContextualHero no playground interativo do Storybook:

<StorybookEmbed
  storyId="components-internalcontextualhero--usage"
  height={400}
  title="InternalContextualHero - Playground Interativo"
/>

## Documentação Completa

Para documentação técnica detalhada, exemplos de uso e API completa, consulte o **[Storybook do InternalContextualHero](https://ocean-ds.github.io/ocean-web/?path=/docs/components-internalcontextualhero--docs)**.

No Storybook você encontrará:

- ✅ **Controles interativos** para testar todas as props
- ✅ **API gerada automaticamente** com tipagem completa
- ✅ **Exemplos visuais** de todas as variantes e estados
- ✅ **Playground** para experimentação rápida

## Exemplos de Uso

### Completo

Hero com todas as propriedades preenchidas:

<StorybookEmbed
  storyId="components-internalcontextualhero--all-variants"
  height={800}
  showToolbar={false}
  title="InternalContextualHero - Todas as Variantes"
/>

### Com uma ação

Hero com apenas um botão de ação:

<StorybookEmbed
  storyId="components-internalcontextualhero--with-one-action"
  height={300}
  showToolbar={false}
  title="InternalContextualHero - Uma Ação"
/>

### Sem ações

Hero sem botões de ação:

<StorybookEmbed
  storyId="components-internalcontextualhero--without-actions"
  height={300}
  showToolbar={false}
  title="InternalContextualHero - Sem Ações"
/>

### Sem imagem

Hero sem a seção de imagem:

<StorybookEmbed
  storyId="components-internalcontextualhero--without-image"
  height={300}
  showToolbar={false}
  title="InternalContextualHero - Sem Imagem"
/>

### Com ListAction

Hero utilizando componentes ListAction como itens da lista:

<StorybookEmbed
  storyId="components-internalcontextualhero--with-list-action-items"
  height={350}
  showToolbar={false}
  title="InternalContextualHero - Com ListAction"
/>

### Lista de texto com ícones

Hero com itens de texto simples e ícones:

<StorybookEmbed
  storyId="components-internalcontextualhero--with-text-list-items"
  height={300}
  showToolbar={false}
  title="InternalContextualHero - Lista com Ícones"
/>

### Lista de texto sem ícones

Hero com itens de texto simples sem ícones:

<StorybookEmbed
  storyId="components-internalcontextualhero--with-text-list-items-no-icons"
  height={300}
  showToolbar={false}
  title="InternalContextualHero - Lista sem Ícones"
/>

### Mínimo

Hero apenas com as propriedades obrigatórias:

<StorybookEmbed
  storyId="components-internalcontextualhero--minimal"
  height={250}
  showToolbar={false}
  title="InternalContextualHero - Mínimo"
/>

## API

### Props

| Prop          | Tipo                                                                                             | Padrão | Descrição                                                                                     |
| ------------- | ------------------------------------------------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------- |
| `title`       | `string`                                                                                         | -      | **Obrigatório.** Título principal do hero.                                                    |
| `description` | `string`                                                                                         | -      | **Obrigatório.** Descrição ou subtítulo do hero.                                              |
| `listItems`   | `(ReactNode \| InternalContextualHeroListItemString)[]`                                          | -      | **Obrigatório.** Lista de itens a serem exibidos.                                             |
| `image`       | `string \| ReactNode`                                                                            | -      | Imagem ou componente a ser exibido na lateral direita. Se for string, renderiza como `<img>`. |
| `actions`     | `[InternalContextualHeroAction] \| [InternalContextualHeroAction, InternalContextualHeroAction]` | -      | Botões de ação (máximo 2). O primeiro usa variant `primary`, o segundo usa `tertiary`.        |
| `className`   | `string`                                                                                         | -      | Classes CSS adicionais.                                                                       |

### Tipos

#### InternalContextualHeroListItemString

```tsx
type InternalContextualHeroListItemString = {
  icon?: ReactNode;
  description: string;
};
```

#### InternalContextualHeroAction

```tsx
type InternalContextualHeroAction = {
  label: string;
  onClick: () => void;
};
```

## Estrutura de listItems

O prop `listItems` aceita dois tipos de itens:

### 1. Objeto com descrição (e ícone opcional)

```tsx
listItems={[
  { icon: <CheckCircleOutline size={20} />, description: 'Item com ícone' },
  { description: 'Item sem ícone' },
]}
```

### 2. ReactNode customizado

```tsx
listItems={[
  <ListAction key="1" title="Item 1" description="Descrição" type="text" />,
  <CustomComponent key="2" />,
]}
```

## Acessibilidade

- ✅ Utiliza elementos semânticos HTML
- ✅ Botões de ação são elementos `<button>` nativos
- ✅ Imagem inclui atributo `alt` automaticamente (usando o título)
- ✅ Suporte a navegação por teclado
- ✅ Suporte a ref forwarding

## Boas práticas

- Use títulos claros e concisos
- A descrição deve complementar o título, não repeti-lo
- Limite a lista a 3-5 itens para melhor legibilidade
- Use no máximo 2 botões de ação para evitar sobrecarga visual
- A imagem deve ser relevante ao contexto do hero

## Links relacionados

- [Button](/components/button)
- [Typography](/components/typography)
- [ListAction](/components/listaction)

