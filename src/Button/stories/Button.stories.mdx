import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Button from '../Button';

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    component: {
      name: 'component',
      description:
        'The component used for the root node. Either a string to use a HTML element or a component.',
      defaultValue: 'button',
      table: {
        type: {
          summary: 'elementType',
        },
        defaultValue: {
          summary: 'button',
        },
      },
      control: {
        type: null,
      },
    },
    variant: {
      name: 'variant',
      description: 'The variant to use.',
      defaultValue: 'primary',
      table: {
        type: {
          summary: '"primary" | "secondary" | "ghost" | "inverse"',
        },
        defaultValue: {
          summary: 'primary',
        },
      },
      control: {
        type: null,
      },
    },
    size: {
      name: 'size',
      description: 'The size of the button.',
      defaultValue: 'md',
      table: {
        type: {
          summary: '"sm" | "md" | "lg"',
        },
        defaultValue: {
          summary: 'md',
        },
      },
      control: {
        type: null,
      },
    },
    fullWidth: {
      name: 'fullWidth',
      description:
        'If true, the button will take up the full width of its container.',
      defaultValue: false,
      table: {
        type: {
          summary: 'true | false',
        },
        defaultValue: {
          summary: false,
        },
      },
      control: {
        type: null,
      },
    },
  }}
/>

# Button API

The API documentation of the Button React component. Learn more about the props and the CSS customization points.

## Import

```javascript
import { Button } from '@useblu/ocean-components';
```

## Usage

<Canvas withSource="open" withToolbar>
  <Story name="Usage">
    <Button>Click Here!</Button>
  </Story>
</Canvas>

## Props

<ArgsTable of="." showComponent={false} />

The ref is forwarded to the root element.

Any other props supplied will be provided to the root element (native element).

## CSS

| Global class         | Description                          |
| -------------------- | ------------------------------------ |
| .ods-btn             | Styles applied to the root element.  |
| .ods-btn--primary    | The color of the primary component   |
| .ods-btn--secondary  | The color of the secondary component |
| .ods-btn--ghost      | The color of the ghost component     |
| .ods-btn--inverse    | The color of the inverse component   |
| .ods-btn--[sm,md,lg] | The width set to the component       |
| .ods-btn--full-width | Full width to the component          |

If that's not sufficient, you can check the [implementation of the component](https://github.com/Pagnet/ocean-ds-web/blob/master/src/Button/Button.tsx) for more detail.

## Playground

<Canvas>
  <Story
    name="Playground"
    args={{ children: 'My Button', fullWidth: false }}
    argTypes={{
      children: {
        name: 'children',
        control: 'text',
      },
      variant: {
        name: 'variant',
        control: {
          type: 'select',
          options: ['primary', 'secondary', 'ghost', 'inverse'],
        },
      },
      size: {
        name: 'size',
        control: {
          type: 'select',
          options: ['sm', 'md', 'lg'],
        },
      },
      fullWidth: {
        name: 'fullWidth',
        control: 'boolean',
      },
    }}
  >
    {(props) => <Button {...props} />}
  </Story>
</Canvas>

<ArgsTable story="Playground" of="." />
