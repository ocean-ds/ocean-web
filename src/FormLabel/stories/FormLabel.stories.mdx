import { Meta, ArgsTable } from '@storybook/addon-docs/blocks';
import FormLabel from '../FormLabel';

<Meta title="Components/FormLabel" component={FormLabel} />

# FormLabel API

The API documentation of the FormLabel React component. Learn more about the props and the CSS customization points.

## Props

<ArgsTable of="." />

## CSS

| Global class              | Description                                            |
| ------------------------- | ------------------------------------------------------ |
| .ods-form-label           | Styles applied to the root element.                    |
| .ods-form-label--disabled | Styles applied to the root element if disabled={true}. |

If that's not sufficient, you can check the [implementation of the component](https://github.com/Pagnet/ocean-ds-web/blob/master/src/FormLabel/FormLabel.tsx) for more detail.
