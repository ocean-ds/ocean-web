import React from 'react';
import Typography from '../Typography';

export default {
  title: 'Typography',
};

export const Heading: React.FunctionComponent = () => (
  <Typography variant="heading1">Headings</Typography>
);
